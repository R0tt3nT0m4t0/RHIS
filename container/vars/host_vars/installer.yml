# installer.yml — generated by run_rhis_install_sequence.sh
ansible_user: "{{ installer_user | default('sgallego') }}"
aap_remote_user: "{{ admin_user | default('admin') }}"
ansible_ssh_private_key_file: "{{ rhis_installer_ssh_private_key_file | default('~/.ssh/rhis-installer/id_rsa') }}"
