:foreman:
  :host: "{{ satellite_server_url | default('https://' ~ (ansible_fqdn | default(inventory_hostname))) }}"
  :username: "{{ hammer_username | default(satellite_username | default(admin_user | default('admin'))) }}"
  :password: "{{ hammer_password | default(satellite_password | default(sat_admin_pass | default(global_admin_password | default(admin_pass | default(''))))) }}"
